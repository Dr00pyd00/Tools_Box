
# Exemple

On creer un projet simple python avec postgres / python.   
Il faudra donc:

- une image postgresql ( dispo officiel )
- une image python particuliere avec des requirements speciaux : donc il faut la build nous meme avec un `dockerfile` precis


